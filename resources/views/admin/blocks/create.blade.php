@if (!empty($href)) <a href="{{$href}}"> @endif
    <button type="button" class="btn btn-outline-primary btn-sm waves-effect waves-light get-modal-add" data-toggle="modal" data-target="#add-modal">Создать</button>
    @if (!empty($href)) </a> @endif